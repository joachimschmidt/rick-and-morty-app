import 'package:flutter/material.dart';

import 'locations_list/locations_list.dart';

class LocationsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return LocationsList();
  }
}
